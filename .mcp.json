{
  "mcpServers": {
    "odoo": {
      "command": "npx",
      "args": ["-y", "mcp-odoo-adv"],
      "env": {
        "ODOO_URL": "${ODOO_URL}",
        "ODOO_DB": "${ODOO_DB}",
        "ODOO_USERNAME": "${ODOO_USERNAME}",
        "ODOO_PASSWORD": "${ODOO_API_KEY}"
      }
    }
  }
}
